# Thermogram Pre-processing App — Session Memory

## Goal
Add a proper independent second Y-axis (right side) displaying real mass values instead of the fake scaled axis in thermogram plots.

## Constraints & Preferences
- Keep `_mass_scale()` function in code for compatibility
- TG traces should use `yaxis="y2"` with real mass values
- Right Y-axis must remain visible even when TG trace is hidden
- No visual regression on existing axes (left Y, bottom X)

## Progress
### Done
- Updated `build_main_plot()` and `build_raw_plot()` to place TG on `yaxis="y2"` with real mass values
- Removed `mass_scale` parameter from `_add_tg_inflection_markers()` calls
- Fixed `figure_to_json()` to serialize `yaxis: "y2"` for traces on secondary axis
- Fixed `range: null` bug by conditionally adding range only when mass data exists
- Added invisible anchor trace on y2 when TG is hidden to force Plotly to render the right axis
- Verified right Y-axis remains visible with tick labels in all toggle states (TG visible/hidden)
- Full browser test passed with zero console errors

### In Progress
- (none)

### Blocked
- (none)

## Key Decisions
- Use `yaxis2_cfg` dict pattern to conditionally include `range` instead of passing `None` (which broke Plotly axis rendering)
- Keep `_mass_scale()` function in codebase per user request, just stop using it for TG plotting
- Add invisible trace on y2 when TG is hidden to force Plotly to render the right axis

## Next Steps
- Consider adding a separate TGA2/raw plot tab (currently `build_raw_plot` exists but is not wired to any route)
- Consider implementing `/export/plot` endpoint (currently returns 501 Not Implemented)

## Critical Context
- Plotly requires at least one trace assigned to an axis to render it; `range` alone is insufficient
- `figure_to_json()` was not serializing `yaxis` field because default `yaxis=None` failed the truthiness check
- Frontend `normalizeFigure()` expects `{data, layout}` format; initial page load sends different format but that's handled separately
- Test file: `/home/einhander/Documents/4_Projects/tg-py/samples/Сосна/Сосна 600_10_250мг.dat`

## Relevant Files
- `/home/einhander/Documents/4_Projects/tg-py/src/tgapp/infrastructure/plotting.py`: Main plotting logic with `build_main_plot()`, `build_raw_plot()`, `figure_to_json()`, `_add_tg_inflection_markers()`
- `/home/einhander/Documents/4_Projects/tg-py/src/tgapp/web/static/app.js`: Frontend Plotly rendering with `normalizeFigure()` and `renderPlot()`
- `/home/einhander/Documents/4_Projects/tg-py/src/tgapp/web/templates/partials/plot_block.html`: Template embedding plot JSON
